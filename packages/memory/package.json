{
  "name": "@continuum/memory",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical memory engine: lifecycle, validity, suggestions, contradiction handling, and injection-safe candidate extraction.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@continuum/auth": "workspace:*",
    "@continuum/contracts": "workspace:*"
  },
  "devDependencies": {
    "@continuum/testing": "workspace:*",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
