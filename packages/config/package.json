{
  "name": "@continuum/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript and tooling configuration for the Continuum monorepo. Changes require review from a second teammate.",
  "files": [
    "tsconfig.base.json",
    "tsconfig.node-package.json"
  ],
  "scripts": {
    "build": "echo 'config: nothing to build'",
    "lint": "echo 'config: nothing to lint'",
    "typecheck": "echo 'config: nothing to typecheck'",
    "test": "echo 'config: no tests'"
  }
}
