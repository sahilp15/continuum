{
  "name": "@continuum/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Continuum design tokens and shared UI primitives, consumed by the web app and browser extension.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./tokens.css": "./src/tokens.css"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'ui: no tests yet'"
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
