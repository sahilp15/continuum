{
  "name": "@continuum/app-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Background worker: PostgreSQL-backed job queue for connector sync, extraction, and maintenance jobs.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "dev": "node --experimental-strip-types src/main.ts"
  },
  "dependencies": {
    "@continuum/contracts": "workspace:*",
    "@continuum/db": "workspace:*",
    "@continuum/observability": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
